apiVersion: v2
name: frontend
description: A Helm chart for the Next.js frontend application
type: application

# Chart version (Helm chart packaging)
version: 0.1.0

# Application version (your Docker image version)
appVersion: "1.0.0"
